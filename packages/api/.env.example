# ChatSDK API Environment Configuration
# Copy this file to .env and fill in your values

# Server
PORT=5500

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5433
DB_USER=chatsdk
DB_PASSWORD=your-password
DB_NAME=chatsdk

# Centrifugo (WebSocket)
CENTRIFUGO_API_URL=http://localhost:8000/api
CENTRIFUGO_API_KEY=chatsdk-api-key-change-in-production
CENTRIFUGO_TOKEN_SECRET=chatsdk-dev-secret-key-change-in-production

# Novu (Push Notifications)
# Get from Novu dashboard: http://localhost:4200 → Settings → API Keys
NOVU_SECRET_KEY=
NOVU_API_URL=http://localhost:3000

# Multi-tenant support: prefix subscriber IDs for isolation across environments
# Example: "hello" creates subscriber IDs like "hello:user123"
# Leave empty for single-tenant deployments (backwards compatible)
# NOVU_TENANT_ID=

# Custom workflow IDs (optional - defaults shown)
# Use these to align with existing Novu workflows in your main app
# NOVU_WORKFLOW_NEW_MESSAGE=new-message
# NOVU_WORKFLOW_MENTION=mention
# NOVU_WORKFLOW_CHANNEL_INVITE=channel-invite
# NOVU_WORKFLOW_REACTION=reaction
# NOVU_WORKFLOW_THREAD_REPLY=thread-reply

# Inngest (Background Jobs)
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=
