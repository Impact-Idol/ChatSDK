# ChatSDK 2.0 Minimal Production Configuration
# Only 3 variables required! Everything else auto-configures.

# Database (REQUIRED)
DATABASE_URL=postgresql://user:password@host:5432/database

# JWT Secret (REQUIRED) - Generate with: openssl rand -base64 32
JWT_SECRET=your-secret-key-here-change-this

# Centrifugo Token Secret (REQUIRED) - Generate with: openssl rand -base64 32
CENTRIFUGO_TOKEN_SECRET=your-centrifugo-secret-here

# That's it! ChatSDK 2.0 handles the rest automatically.
