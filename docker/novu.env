# Novu Environment Variables
# Copy this file and fill in production values for deployment

# JWT Secret - CHANGE IN PRODUCTION
JWT_SECRET=chatsdk-novu-jwt-secret-change-in-production-min-32-chars

# Encryption key for sensitive data (must be exactly 32 characters)
STORE_ENCRYPTION_KEY=chatsdknovuencryptkey32charlongx

# Storage configuration (using MinIO)
S3_LOCAL_STACK=http://minio:9000
S3_BUCKET_NAME=novu
S3_REGION=us-east-1
AWS_ACCESS_KEY_ID=chatsdk
AWS_SECRET_ACCESS_KEY=chatsdk_dev_123

# Disable new relic and other optional integrations
NEW_RELIC_ENABLED=false
SEGMENT_TOKEN=

# Disable telemetry for local development
DISABLE_USER_REGISTRATION=false
