# Current Mission

## 2026-06-19

- Added shared-server integration instructions for teams building apps against the LAN ChatSDK deployment.
- Began wiring ChatSDK into the same Graphify and Mempalace operating pattern used by `/Users/pushkar/vnet`.
- Added `docs/features/vouch-integration/INTEGRATION_PACKET.md` to translate the shared-server guide into a Vouch-specific integration plan without modifying `/Users/pushkar/vnet`.
- Added Vouch starter adapters and `npm run smoke:shared-server`; live smoke passed 9/9 against the LAN deployment with token minting, WebSocket connect, DM creation, message send, and message query.
- Confirmed client checklist: env URLs and token shape are correct; stable user IDs, deterministic DM reuse, nonmember read/write/realtime denial, and removed-member denial all pass live probes. Current browser-origin allowlist includes `localhost:3000` and demo UI, but not `localhost:4500`, `localhost:3001`, or `https://vouch.vedalogy.com`.
- Applied Vouch origin allowlist on the Proxmox-hosted shared deployment at `192.168.68.244` under `/opt/chatsdk/deploy`; recreated API, Centrifugo, and token broker. Post-restart CORS/token/WebSocket origin probes passed for all Vouch origins, and `npm run smoke:shared-server` passed 9/9.
- Implemented and deployed app-authenticated user bootstrap endpoints for Vouch: `POST /api/users/ensure` and `POST /api/users/bulk-ensure`. Local API tests/build passed; live Proxmox verification confirmed create, existing, update, bulk backfill, and bearer-token rejection behavior.
- Implemented and deployed Vouch server-side DM authorization contract: `POST /api/channels/dm/ensure` with `X-API-Key`, `channel:create` split from `chat:write`, LAN token broker scoped browser tokens without `channel:create`, and scoped refresh preservation. Live verification confirmed server DM create/open, browser channel-create denial, and message send inside server-created DM.
- Completed GPT-5.5 High and Claude security review of the Vouch ChatSDK auth/channel changes. Fixed unscoped-token fail-open, ambiguous/oversized DM CIDs, concurrent DM ensure idempotency, idempotency-key mismatch, DM ensure body/rate limits, DM member/role mutation bypass, and member-visible custom config. Deployed to `192.168.68.244`; final live security probe and 10/10 smoke passed.
- Provisioned a dedicated live ChatSDK app for Vouch: app ID `546aff6b-d3be-4dec-819b-576b42362ea9`. Server-only key is stored locally at `.secrets/vouch-chatsdk-app-api-key.env` and not recorded in docs/chat. LAN token broker is pinned to this Vouch app; live verification confirmed user ensure, idempotent DM ensure, Vouch-origin token mint with the Vouch app ID, and browser channel-create denial.
- Implemented and deployed server-side Vouch group/squad ensure endpoints: `POST /api/channels/group/ensure` and `POST /api/channels/squad/ensure`. They require app `X-API-Key`, reject browser bearer tokens, require deterministic external IDs and existing members, create or return existing group channels, and leave Vouch policy enforcement outside ChatSDK. Live verification passed for squad create/reuse, group create, missing-user rejection, bearer denial, browser channel-create denial, and 10/10 shared-server smoke.
- Added `docs/runbooks/proxmox-lan-code-deploy.md` as the code-only deployment runbook for the LAN host `192.168.68.244`, covering preflight checks, code sync, additive migrations, service rebuild/restart, health checks, smoke verification, config backup, rollback, and post-deploy records.
- Recorded Vouch channel membership lifecycle decision: when a user leaves a squad or loses an org role, Vouch should remove them from the ChatSDK private/group channel. Current ChatSDK does not provide a separate former-member/read-only history role; implementing history-visible/no-send access would require a new product/security design.
- Started the multi-project onboarding hardening run at `docs/agent-runs/2026-06-20-multi-project-onboarding-hardening/`. New feature plan lives in `docs/features/multi-project-onboarding-hardening/`. Slice 0 selected a CLI/operator workflow first, using `app_api_key` and broker scope primitives with redacted output, before any admin UI expansion.
- Implemented Slice 1 app/credential provisioning: `npm run ops:provision:project` now supports project create/show/rotate-key using `app_api_key`, redacted output by default, opt-in `--emit-secret`, transactions, advisory locks, idempotent metadata preservation, and rotation typo protection. Admin app create now emits an app-scoped key; primary-key regeneration is deprecated. Focused provisioning tests pass 8/8; API typecheck remains blocked by pre-existing `packages/api/src/routes/users.ts` typing.
- Completed Slice 2 broker scope provisioning and auth hardening: `provision-broker` creates broker clients, RS256 credentials, and app scopes with exact origins, nonempty tenant/user/channel constraints, redacted private JWK output by default, idempotent omitted-flag preservation, and refusal to reactivate suspended/disabled broker rows. Broker membership scope checks now fail closed on empty constraints; app API key auth uses `app_api_key` by default with primary-key fallback only behind `CHATSDK_ENABLE_PRIMARY_APP_KEY_AUTH=true`. Claude and Antigravity post-fix rereviews found no unresolved critical/high issues. API typecheck passes; full API suite passed 32 files with 282 tests and 7 skips.
- Advanced Slice 3 generic membership sync evidence: added tests proving removed broker membership cannot mint, removed status deletes channel memberships without deleting messages, and stale active snapshots cannot resurrect newer removed membership state. API typecheck passes; full API suite passed 32 files with 285 tests and 7 skips.
- Advanced Slice 4 project smoke CLI: added `npm run smoke:project` with CLI/env/config support, redacted `--dry-run --json`, optional app-key user ensure, token mint, WebSocket connect, deterministic DM ensure/open, scoped browser channel-create denial, message send, and readback. Added project-smoke helper tests and onboarding runbook. API typecheck passes; full API suite passed 33 files with 288 tests and 7 skips.
- Ran live generic project smoke against the LAN/Vouch shared server using `.secrets/vouch-chatsdk-app-api-key.env` and origin `https://vouch.vedalogy.com`; 12/12 checks passed including user ensure, token mint, WebSocket connect, deterministic DM ensure/open, browser channel-create denial, message send, and readback.
- Resolved final-review blockers for multi-project hardening: app-scoped API keys now store/authenticate by SHA-256 hash, primary app-key fallback is opt-in only, `requireScope` fails closed for unscoped user tokens, broker route restrictions apply only to actual broker tokens, signed media tokens carry `chat:read`, and membership sync preserves existing display names when `displayName` is omitted. Post-fix focused tests passed 107/107, API typecheck passed, and the full API suite passed 289/296 with 7 skips.
- Claude and Antigravity post-blocker rereviews both returned GO with no critical/high blockers. After rereview, removed the dead `api_key` column from the new app-scoped-key migration and changed project `show` output to display a non-secret `sha256:` key fingerprint. Final API typecheck and full API suite still pass; Graphify refreshed to 8702 nodes, 11395 edges, 560 communities, and 570 wiki articles.
- Deployed the completed multi-project hardening changes to the LAN ChatSDK stack at `192.168.68.244`. Rebuilt `chatsdk-hardening-api:local`, applied V014/V015 live DB compatibility changes on `192.168.68.242`, configured `BROKER_DATABASE_URL`, recreated API and token broker, and verified `/health`, `/ready`, token broker health, and Vouch project smoke 12/12.
- Responded to Vouch's 2026-06-30 browser messaging handoff request from `/Users/pushkar/vnet/docs/features/chatsdk-vouch-dm/CHATSDK_HANDOFF_REQUEST.md`: `@chatsdk/core` and `@chatsdk/react` now package from `dist`, React prebuilds core, React depends on `@chatsdk/core@2.0.8`, channel member payloads include user `custom` metadata, and `docs/features/vouch-integration/HANDOFF_RESPONSE_2026-06-30.md` captures the stable browser/app-auth contracts. Verification passed package builds, pack dry-runs, targeted API tests, and Graphify update. Remaining gate is publishing/providing reproducible `2.0.8` package artifacts.
- Expanded the Vouch verification gate before handoff: fixed packed ESM relative specifiers, added `zod` as a direct `@chatsdk/core` dependency, installed fresh `2.0.8` tarballs into a blank consumer app, and confirmed TypeScript plus runtime ESM imports pass. Added private-data tests proving nonmembers are denied before private message, receipt/read-state, thread, save, and poll data queries/writes. Added `npm run smoke:private-isolation`; live shared-server verification passed project smoke 12/12 and private-isolation smoke 16/16, proving a third user cannot list/read/send/read-status another pair's DM. Latest local checks pass: API 298/305 with 7 skips, private-data 37/37, core 351/351, React build, React 43/43, and Graphify update. Remaining acceptance limit is a live Vouch authenticated browser flow.
